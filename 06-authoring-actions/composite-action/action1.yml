name: Hello_world
describtion: Greet_someone

inputs:
  who_to_greet:
    describtion: who_to_greet
    required: true
    defaults: "World"
  runs:
    using: composite
    steps:
      - run: echo " Hello $who_to_greet!!!"
        shell: bash
        env: 
          Input_who_to_Greet: ${{inputs.who_to_greet}}