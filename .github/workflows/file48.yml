name: check_concurrency
on:
  workflow_dispatch: 
concurrency:
    group: Production
    cancel-in-progress: true
jobs:
  Test_concurrency:
    runs-on: ubuntu-latest
    steps:
      - name: sleep
        timeout-minutes: 1
        run: |
         echo "Sleeping...."
         sleep 120

   
