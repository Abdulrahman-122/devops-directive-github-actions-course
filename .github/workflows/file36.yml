name: npm_cache
on:
  workflow_dispatch:
jobs:
  Check_npm:
    runs-on: ubuntu-latest
    steps:
     - uses: actions/checkout@v5
     - uses: actions/setup-node@v4
       with:
        node-version: 20
        cache: npm
     - run: npm --version

