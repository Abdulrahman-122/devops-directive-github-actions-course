name: check_some_info
on:
  push:
  pull_request: 
  workflow_dispatch: 
jobs:
  test:
    runs-on: ubuntu-latest
    steps:
      - run: echo "we need to know important stuff from that workflow"
      - run: echo $GITHUB_EVENT_NAME
      - run: echo $GITHUB_REF
      - run: echo $GITHUB_SHA
      - run: echo $GITHUB_REPOSITORY
      - run: echo $GITHUB_ACTOR